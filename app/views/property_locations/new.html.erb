<h1> Cadastro de região </h1>

<%= form_with(model: @property_location) do |f|%>
    <div>
        <h3><%= f.label :title, 'Nome'%><h3>
    </div>
    <div style="margin-top: 25px; margin-bottom: 25px">
        <%= f.text_field :title %>
        <%= f.submit 'Enviar' %>
    </div>
<% end %>

<% if @property_location.errors.any? %>
    <%= @property_location.errors.full_messages %>
<% end %>

<div> <%= link_to 'Voltar', root_path %> </div>